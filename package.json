{
  "name": "mia-shuttles",
  "private": true,
  "version": "0.1.0",
  "description": "Landing page for Mia Shuttles - private transportation service in Israel.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
